# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.local

# Reference code — local only, never commit
oldProjectData/
lessonMarkdown/
lessonMarkdawn/

# Claude Code local config (skills, permissions, 10x toolkit)
.claude/
.claude/**/10x-*
.github/**/10x-*
.agents/**/10x-*

# tach generated artifacts
tach_module_graph.dot

# Node / TS (tools/ai-code-reviewer) — dist/ already covered by the unanchored rule above
tools/**/node_modules/
tools/**/*.tsbuildinfo

# Exported GCP service-account / Cloud Run credential dumps — never commit
puls-gpw-api-*.json

# Personal broker portfolio screenshots (PUL-39) — financial data, never commit
broker_data/

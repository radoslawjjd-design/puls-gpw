[project]
name = "puls-gpw"
version = "0.1.0"
description = "ESPI/EBI analyzer for GPW and NewConnect announcements"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
]
